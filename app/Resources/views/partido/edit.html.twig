{% extends 'layout/base.html.twig' %}

{% block body_id 'partido_edit' %}

{% block main %}
    <div class="h4">Editar partido</div>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}

        <input type="submit" value="Guardar cambios" class="btn btn-primary" />
        <a class="btn btn-danger" href="{{ path('partido_index') }}">Cancelar</a>
    {{ form_end(edit_form) }}
{% endblock %}
