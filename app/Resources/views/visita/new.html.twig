{% extends 'base.html.twig' %}

{% block body_id 'visita_new' %}

{% block main %}
    <h1>Registrar visita</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Guardar" class="btn btn-primary" />
        <a class="btn btn-link" href="{{ path('visita_index', {'id': paciente.id}) }}">Volver a listado</a>
    {{ form_end(form) }}

{% endblock %}
