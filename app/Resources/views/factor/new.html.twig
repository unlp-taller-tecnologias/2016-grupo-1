{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo factor de riesgo</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('factor_index') }}">Volver al listado</a>
        </li>
    </ul>
{% endblock %}
