{% extends 'layout/base.html.twig' %}

{% block body_id 'paciente_edit' %}

    {% block main %}
        <h1>Editar paciente</h1>

        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}

        <label class="control-label">Localidad</label>
        <div class="input-group">
            <input type="text" placeholder="Haga clic en el botón buscar para seleccionar una localidad" id="paciente_localidad_text" class="form-control disabled" readonly="true">
            <span class="input-group-btn">
                <button type="button" class="btn btn-primary" id="boton-buscar-localidad">
                    <i class="fa fa-search"></i>
                </button>
            </span>
        </div>
        <input type="submit" value="Guardar cambios" class="btn btn-primary" />
        <a class="btn btn-danger" href="{{ path('paciente_show', {'id': paciente.id}) }}">Cancelar</a>
        {{ form_end(edit_form) }}

        {{ include('_select_localidad.html.twig', {
                partidos: partidos
            }) }}
    {% endblock %}
