{{ form_start(busqueda, { attr: attr }) }}
    {{ form_widget(busqueda) }}
    <button type="submit" class="btn btn-primary {{ button_css|default("") }}">
        <i class="fa fa-search" aria-hidden="true"></i> {{ button_label|default('Buscar') }}
    </button>
{{ form_end(busqueda) }}
