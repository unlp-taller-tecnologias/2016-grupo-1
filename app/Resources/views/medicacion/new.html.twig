{% extends 'base.html.twig' %}

{% block body_id "admin_medicacion_new" %}

{% block main %}
    <h1>Nueva opción de medicaciones para examen prequirúrgico</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Guardar" class="btn btn-primary" />

        <a class="btn btn-link" href="{{ path('medicacion_index') }}">Volver a listado</a>
        {{ form_end(form) }}
{% endblock %}
