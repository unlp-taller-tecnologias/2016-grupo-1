{% extends 'base.html.twig' %}

{% block main %}
  <h1>{{ diagnostico.diagnostico }}</h1>

    <a class="btn btn-link" href="{{ path('diagnostico_index') }}">Volver al listado</a>

{% endblock %}

{% block sidebar %}
    <div class="section">
        <a href="{{ path('diagnostico_edit', { 'id': diagnostico.id }) }}" class="btn btn-lg btn-block btn-success">
            <i class="fa fa-edit" aria-hidden="true"></i> Editar diagn&oacutestico
        </a>
    </div>

    <div class="section">
        {{ include('_delete_form.html.twig', {
            form: delete_form,
            button_label: 'Eliminar diagnóstico',
            button_css: 'btn-lg btn-block',
        }) }}
    </div>

    {{ parent() }}
{% endblock %}
