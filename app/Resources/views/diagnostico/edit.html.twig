{% extends 'base.html.twig' %}


{% block main %}
    <h1>Editar diagn&oacutestico</h1>

    {{ form_start(edit_form) }}
        {{ form_row(edit_form.diagnostico) }}

        <input type="submit" value="Guardar cambios" class="btn btn-primary" />
        <a class="btn btn-link" href="{{ path('diagnostico_index') }}">Volver al listado</a>
    {{ form_end(edit_form) }}
{% endblock %}
