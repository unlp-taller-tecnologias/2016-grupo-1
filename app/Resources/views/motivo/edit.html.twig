{% extends 'layout/base.html.twig' %}

{% block body_id 'motivo_edit' %}

{% block main %}
    <div class="h4">Editar motivo de consulta</div>

    {{ form_start(edit_form) }}
        {{ form_row(edit_form.motivo) }}

        <input type="submit" value="Guardar cambios" class="btn btn-primary" />
        <a class="btn btn-danger" href="{{ path('motivo_index') }}">Cancelar</a>
    {{ form_end(edit_form) }}
{% endblock %}
